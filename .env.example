# Database Configuration (Local Development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nysc_queue
DB_USER=postgres
DB_PASSWORD=your_password_here

# OR use DATABASE_URL (for Vercel/Cloud deployment)
# DATABASE_URL=postgres://user:password@host:5432/nysc_queue

# Server Configuration
PORT=3000
NODE_ENV=development

# Geofencing: set to true only for local dev to skip location/geofence checks (never use in production)
DEV_MODE=false

# Security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
ADMIN_PIN=1234

# LGA Configuration (Seed Data)
DEFAULT_LGA_NAME=Amuwo-Odofin
DEFAULT_LGA_LAT=6.4706
DEFAULT_LGA_LNG=3.2838
DEFAULT_LGA_RADIUS=500
